#!/bin/bash

#初期化
declare -a arr1=(a b c d e f g)
arr2=(h i j k)
arr3[11]=o
arr3[13]=q
arr4=([1]=t [2]=u [4]=w)
arr5=(A B '' D)

#count
echo ---count---
echo ${#arr1[@]}
echo "${#arr2[@]}"
echo ${#arr3[*]}
echo "${#arr4[*]}"

#echo
echo ---echo---
echo ${arr1[@]}
echo "${arr2[@]}"
echo ${arr3[*]}
echo "${arr4[*]}"

#for in
echo ---for in---
for x in ${arr1[@]};
do
    echo ${x}
done
echo

for x in "${arr2[@]}";
do
    echo ${x}
done
echo

for x in ${arr1[*]};
do
    echo ${x}
done
echo

for x in "${arr1[*]}";
do
    echo ${x}
done
echo

#####
# var_dump
#####
function var_dump(){
    local x
    for x in "$@";
    do
        echo "${x}"
    done

    echo
}

echo ---var_dump---

var_dump a b c d
var_dump ${arr1}
var_dump "${arr2}"
var_dump ${arr1[@]}
var_dump "${arr2[@]}"
var_dump ${arr1[*]}
var_dump "${arr2[*]}"





